# Optional but required if Online mode should work in a hosted web build.
# Deploy server/online-server.mjs to a WebSocket-capable host, then set:
VITE_ONLINE_SERVER_URL=wss://your-online-server.example
